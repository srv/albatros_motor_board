<package>
  <description brief="albatros_motorboard">

     This package contains several nodes to manage a motorboard provided by
     Albatros. The motorboard is equiped with sensors (water in and pressure)
     and four motors. Communications with the motorboard are through a 
     serial port. The main node is for monitoring the current status of 
     sensors and motors and providing services to set its configurable 
     parameters. Other nodes provide low and high level navigation control.

  </description>
  <author>Joan Pau Beltran</author>
  <license>GPL</license>
  <review status="unreviewed" notes=""/>
  <url>http://ros.org/wiki/albatros_motorboard</url>
  <export>
    <cpp cflags="-I${prefix}/include" lflags="-Wl,-rpath,${prefix}/lib -L${prefix}/lib -lmotorboardctrl" />
  </export>
  <depend package="roscpp"/>
  <depend package="std_msgs"/>
  <depend package="driver_base"/>
  <depend package="dynamic_reconfigure" />

</package>


